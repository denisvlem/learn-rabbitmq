server:
  port: 8080

spring:
  rabbitmq:
    host: rabbitmq
    port: 5672

management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true